# 安全策略与授权使用声明

## 授权使用（最重要的一节）

`surface-watch` 是一个**只应对你自有资产或已获书面授权资产**使用的检查工具。

使用本工具前，你必须确认满足以下**至少一条**：

1. **你是该资产的合法所有者**（例如你自己的域名、你的服务器）
2. **你持有资产所有者签署的书面授权**，且授权范围明确覆盖你将要检查的目标

**明确禁止**的行为（不限于）：

- 对任何未获授权的第三方系统发起检查
- 把本工具当作攻击前的踩点工具，配合未授权入侵行为使用
- 绕过本工具的 Scope Gate（例如伪造 `scope.json` 的授权声明）对他人资产进行检查
- 对目标使用本工具未提供的破坏性能力（如压力测试、漏洞利用）

> 本工具在代码层面强制拒绝内网、回环、链路本地与云元数据地址，
> 但这只是**工程上的兜底**，不免除使用者的法律责任。
> **未授权的安全测试在多数司法辖区属于违法行为。**

作者不对任何未授权使用或由此产生的后果承担责任。

## 设计上的安全考量

| 考量 | 实现 |
|---|---|
| 越权目标防护 | `src/lib/scope.mjs` 中的 `FORBIDDEN_HOST_PATTERNS` 硬编码拒绝内网/回环/元数据地址 |
| 范围强制 | 每个目标在采集前与扫描入口各校验一次（双重保险） |
| 限速 | 并发上限 8、请求间隔上限 5000ms、超时上限 30s，且均有限值钳制 |
| 默认被动 | 主动路径探测默认关闭，需在 `scope.json` 显式开启 |
| 无凭据处理 | 本工具不接收、不存储、不发送任何账号口令或密钥 |
| 证据脱敏 | 演示夹具中的密钥值均为 `REDACTED_FOR_DEMO`，真实扫描报告可能包含敏感片段，请谨慎公开 |

## 报告数据的敏感性

本工具产出的报告包含**资产清单、证书信息、响应头、以及可能的暴露路径**。
这类信息对攻击者同样有价值。

建议：
- 不要把真实资产的完整报告提交到公开仓库
- 如需公开，请先移除具体域名、IP、路径与证书细节
- 本仓库中的 `fixtures/` 全部使用 RFC 2606 保留域名与 RFC 5737 文档用 IP，可安全公开

## 漏洞报告

如果你在本项目中发现了安全问题（例如 Scope Gate 可被绕过），请不要公开提交 Issue，
而是通过以下方式私下联系：

- 通过 GitHub 私信 / 在 Issue 中仅说明"存在安全问题，需要私下沟通渠道"（不披露细节）

我会在确认后尽快修复，并在 CHANGELOG 中致谢（若你愿意署名）。

## 支持的版本

本项目处于早期阶段（0.x），仅对 `main` 分支的最新代码提供安全修复。
